import 'package:flutter/material.dart';

class ServiceFeesScreen extends StatelessWidget {
  const ServiceFeesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Service Fees Screen"),
      ),
    );
  }
}
